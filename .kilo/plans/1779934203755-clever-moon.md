# Plan for Updating AGENTS.md

## Goal
Create a compact AGENTS.md that helps future agents avoid mistakes and ramp up quickly by including only high-signal, repo-specific guidance.

## Steps

### 1. Inspect Existing AGENTS.md
- Read the current AGENTS.md to understand what is already covered (primarily CodeGraph).
- Determine what to preserve (the CodeGraph section is specific and valuable).

### 2. Identify High-Signal Information from Key Sources
#### Read these files in order:
- README.md (for project overview, tech stack, basic commands)
- package.json (for npm scripts, especially non-obvious ones like validation and asset scripts)
- docs/00_BLOCKMANCER_SOURCE_OF_TRUTH_INDEX.md (for SOT reading order and precedence)
- docs/AGENT.md (for agent-specific rules, workflow, and Definition of Done)
- tsconfig.json and vite.config.ts (for build/toolchain quirks if not obvious from scripts)
- Validate that the scripts in package.json match the validation commands mentioned in docs/AGENT.md.

#### Extract:
- Exact developer commands (especially validation, build, test, asset sync, Android commands)
- Required command order (e.g., lint -> typecheck -> test, but note: lint script may not exist; validation scripts are key)
- Monorepo/multi-package boundaries: This appears to be a single package, but note the separation of `src/game/`, `public/assets/`, `docs/`, `scripts/`.
- Framework/toolchain quirks: 
  - Uses Vite with `--force` in dev.
  - Build runs `tsc --noEmit` before `vite build`.
  - Test is a custom smoke test script.
  - Validation scripts for content, metadata, animations, asset sync, and asset variant auditing.
  - Android Capacitor commands.
- Repo-specific style/workflow conventions:
  - Source-of-Truth (SOT) reading order and precedence.
  - Preserve Cascade Gravity, portrait-mobile readability, cheerful tone, save-facing IDs.
  - Content JSON must use asset keys, not raw paths.
  - Exact-frame PNG naming contract for animations.
  - Monster, boss, and asset folder structure rules.
  - Definition of Done and agent workflow from docs/AGENT.md.
- Testing quirks: 
  - The test script runs a remediation smoke test that checks specific content and system expectations.
  - Validation scripts must pass before building.
- Important constraints from existing instruction files: 
  - Preserve the CodeGraph guidance in the current AGENTS.md.
  - Incorporate the SOT rules and agent workflow from docs/AGENT.md.

### 3. Determine What to Include in the New AGENTS.md
Keep only information that answers: "Would an agent likely miss this without help?"

#### Likely to include:
- The CodeGraph section (from existing AGENTS.md) because it's specific to this repo's tooling.
- SOT reading order and precedence (from docs/00_..._INDEX.md and docs/AGENT.md) because it's critical for knowing which docs to trust.
- Essential npm scripts: 
  - `npm run dev` (with `--force`)
  - `npm run build` (which runs tsc --noEmit first)
  - `npm run test` (the smoke test)
  - Validation scripts: `npm run validate:content`, `npm run validate:metadata`, `npm run validate:animations`, `npm run sync:assets`, `npm run audit:asset-variants`
  - Android commands: `npm run android:sync`, `npm run android:open`, `npm run android:build:debug` (after `npm run android:init`)
- Project structure highlights: 
  - `src/game/` for game logic, scenes, systems.
  - `public/assets/` for runtime assets (with canonical subfolders).
  - `docs/` for SOT files.
  - `scripts/` for validation and asset tools.
- Key conventions that differ from defaults:
  - No patch format; modify files in place.
  - Read focused SOT files before changing behavior.
  - Preserve Cascade Gravity and portrait-mobile layout.
  - Use asset keys in JSON, not raw paths.
  - Exact-frame PNG naming for animations.
  - Save compatibility considerations.
- Definition of Done and agent workflow (condensed from docs/AGENT.md sections 27 and 28) because they outline the exact steps an agent should follow.

#### Likely to exclude:
- Generic software advice (e.g., "use TypeScript correctly").
- Long tutorials or exhaustive file trees.
- Obvious language conventions (e.g., TypeScript semicolons).
- Speculative claims.
- Content that is already well-covered by file names or standard practices (e.g., `src/` contains source code).

### 4. Draft the New AGENTS.md
Combine the preserved CodeGraph section with the extracted high-signal information in a clear, concise format using short sections and bullets.

### 5. Validate the Plan
Ensure the plan only involves reading files (no edits) and that the resulting AGENTS.md will be an improvement over the current one.

## Expected Outcome
A new AGENTS.md that is a valuable, compact reference for agents working on this repo.
